# MRZScanner 1.0.7 pins Capybara below 1.0. The latest compatible release
# is pinned to avoid pip backtracking through incompatible GPU constraints.
mrzscanner-docsaid==1.0.7
capybara-docsaid==0.12.0

# Debian Bookworm ships libjpeg-turbo 2.x. PyTurboJPEG 2.x requires
# libjpeg-turbo 3.0+, so keep the latest compatible 1.x client for this image.
PyTurboJPEG==1.8.3
